# PaddleOCR 3.x — PP-StructureV3 (CPU build).
# Pin minor versions; bump deliberately.
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2

# PaddleOCR 3.x stack
# - paddlepaddle 3.x is the runtime (CPU wheel from PaddlePaddle index)
# - paddleocr 3.x exposes PPStructureV3 pipeline
paddlepaddle==3.0.0
paddleocr==3.0.3
